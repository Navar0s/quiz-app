# Node.js / npm / yarn
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# Optional: Manche Teams versionieren es, andere nicht. Für Solo-Projekte oft okay zu versionieren. Wenn du es NICHT willst, nimm es hier auf.
# Vite / Frontend Build-Artefakte
frontend/dist/
# Backend Daten (falls du sie nicht versionieren willst - für den Start vielleicht besser doch, als Backup)
backend/data/songs.json
backend/data/highscores.json
# Docker
.dockerignore
# OS generated files
.DS_Store
Thumbs.db
# Log files
*.log
nginx_debug.log # Deine Nginx-Logdatei
# IDE-spezifische Ordner (Beispiele)
.vscode/
.idea/
